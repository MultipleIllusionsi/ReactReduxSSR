import React from "react";

const Home = () => {
  return (
    <>
      <div>Home component</div>
      <button onClick={() => console.log("hi there")}>Press me!</button>
    </>
  );
};

export default Home;
